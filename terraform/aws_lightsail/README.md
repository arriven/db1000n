# AWS Lightsail deployment

## Requirements

- AWS account
- `terraform` (1.0+) installed

## Deploy

To deploy:

```sh
terraform init
terraform plan
terraform apply
```

## Destroy

To destroy:

```sh
terraform destroy
```
